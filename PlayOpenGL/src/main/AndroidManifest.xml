<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.sawameimei.playopengl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".DrawPointActivity" />
        <activity android:name=".DrawLineActivity" />
        <activity android:name=".DrawTriangleActivity" />
        <activity android:name=".DrawIcosahedronActivity"></activity>
        <activity android:name=".solarSystem.DrawSolarSystemActivity">
        </activity>
        <activity android:name=".sphere.DrawSphereActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>